export type TypeSocial = {
  id: number;
  name: string;
  url: string;
}

export type TypeNavs = {
  title: string;
  path: string;
}

export type TypeServices = {
  service: string;
  description: string;
}

export type TypeEducation = {
  place: string;
  period: string;
  description: string;
}
